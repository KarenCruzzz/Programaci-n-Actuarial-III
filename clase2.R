mtcars
dput(mtcars,file = "")
carrosKaren



#Dump y source
setwd("")
x <- "Software Actuarial III"
y <- data.frame(a=1, b="a")
dump(c("x","y"),file="data.R")
rm(x,y)
source("data.R")

#dump y source trabajan con las instrucciones de còdigo 

#Conexiones
con <- url("https://www.fcfm.buap.mx/")
x <- readLines(con)
x´´´¨{}
writeLines(readLines(con), "FCFM.txt")

´···3·33·


#Creamos un Vector
x <-c("a", "b", "c", "d", "e")
#Veamos el vector
x
¨´´´{´  }

